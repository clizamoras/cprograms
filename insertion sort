#include<stdio.h>
int main(){
int a[10]={1,7,5,3,4};
int n=5;
int temp,i,j;
for(int i=0;i<n;i++){
 temp=a[i];
 j=i-1;
 while(j>=0 && a[j]>temp){
  a[j+1]=a[j];
  j--;
}
a[j+1]=temp;
}
printf("sorted array is :");
for(i=0;i<n;i++){
 printf("%d",a[i]);
}
return 0;
}