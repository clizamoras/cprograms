#include<stdio.h>
int nCr(int n,int r){
 if(r==0||r==n){
   return 0;
}
return nCr(n-1,r-1)+nCr(n-1,r);
int main(){
int n,r,resulr;
printf("Enter the values of n and r);
scanf("%d %d",&n,&r);
result=nCr(n,r);
printf(result);
return 0;
}
