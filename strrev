
#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);  // or use fgets for safety

    strrev(str);  // built-in reverse function in some compilers

    printf("Reversed string: %s\n", str);
    return 0;
}
