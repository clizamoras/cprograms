
#include<stdio.h>
#define colmax 10
#define rowmax 6
main()
{
int row, column, y;
row=1;
do
{
column=1;
do
{
y=row*column; printf(“%4d”,y)
column=column+1;
}
while(column <= colmax);
printf(“\n”);
row=row+1;
}
while(row<=rowmax);
}