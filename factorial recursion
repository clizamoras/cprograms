#include<stdio.h>
int factorial(int n);
int main(){
int f;
f=factorail(n);
printf("Factorail is= %d",f(5));
return 0;
}
int factorial(int n){
int factorial(n-1);
if(n==1){
return 1;
}
return n*factorial(n-1);
}
