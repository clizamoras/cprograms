
#include <stdio.h>

int main() {
    char str[100];
    int i = 0;

    printf("Enter a string: ");
    gets(str);  // Use fgets in modern code for safety

    while (str[i] != '\0') {
        i++;
    }

    printf("Length of the string: %d\n", i);
    return 0;
}
